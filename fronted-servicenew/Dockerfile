# Use a lightweight web server, such as Nginx
FROM nginx:alpine

# Copy the HTML file to the appropriate location in the Nginx container
COPY index.html /usr/share/nginx/html/

# Expose port 8080
EXPOSE 8080
